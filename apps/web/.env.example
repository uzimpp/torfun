# Frontend configuration. Copy from .env.example; never commit this file.
# NEXT_PUBLIC_* values are inlined into the browser bundle — never put a
# secret here.

NEXT_PUBLIC_API_URL=http://localhost:8080
